services:
  app:
    image: ghcr.io/baddate/gitolite-cgit-docker
    container_name: gitolite-cgit
    env_file:
      - .env
    volumes:
      - git:/var/lib/git/
    ports:
      - "22:22"
      - "80:80"
      - "9418:9418"
volumes:
  git:
